# ── Active config (contains generated secrets) ──────────────
config.env


# ── TLS certificates & keys (all generated per-install) ─────
certs/ca.key
certs/ca.crt
certs/ca.srl

# ── JVM truststore (binary, regenerated on install) ─────────

# ── Docker build cache ───────────────────────────────────────
license-checker/__pycache__/
*.pyc

# ── OS / editor noise ────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.bak
